@echo off
REM Copy this file to env.bat and edit the paths for YOUR machine.
REM env.bat is git-ignored and must NOT be committed (it holds local paths).
REM run.bat sources env.bat when present, otherwise falls back to defaults.

set "KAIR_ROOT=C:\Users\WinSa\Documents\KAIR"
set "SHIM_ROOT=C:\Users\WinSa\Documents\deepinv_shim"
set "DRUNET_WEIGHTS=%KAIR_ROOT%\model_zoo\drunet_color.pth"
set "TESTSET_DIR=%KAIR_ROOT%\testsets\set5"
set "PYTHON=%KAIR_ROOT%\.venv\Scripts\python.exe"
